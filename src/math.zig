pub const linalg = @import("math/linalg.zig");
pub const quat = @import("math/quaternion.zig");
pub const camera = @import("math/camera.zig");

pub const Quaternion = quat.Quaternion;

pub const Vec = linalg.Vec;
pub const vec = linalg.vec;
pub const vec2 = linalg.vec2;
pub const vec3 = linalg.vec3;
pub const vec4 = linalg.vec4;
pub const Matrix = linalg.Matrix;

pub const Camera = camera.Camera;
